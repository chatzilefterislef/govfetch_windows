# Gov Document Fetcher

Τοπική web εφαρμογή που κατεβάζει φορολογικά έγγραφα από τα portals της ΑΑΔΕ
(TaxisNet) με αυτοματοποίηση browser. FastAPI + Playwright.

## Εκκίνηση

Ένα σημείο εκκίνησης για όλες τις πλατφόρμες — φτιάχνει venv, εγκαθιστά πακέτα
και Chromium (μόνο την πρώτη φορά), και ανεβάζει τον server:

```bash
python3 run.py
```

Στα Windows: `python run.py`. Μετά η εφαρμογή είναι στο <http://localhost:8000>.

Μέσα από Claude Code υπάρχει έτοιμο preview config στο `.claude/launch.json`
(«gov-doc-fetcher», και «gov-doc-fetcher (windows)» για Windows).

**Μη τρέχεις `uvicorn` απευθείας** και μη βάζεις απόλυτες διαδρομές σε scripts —
το repo χρησιμοποιείται από πολλά μηχανήματα.

## Δομή

- `main.py` — FastAPI: `/api/start`, SSE πρόοδος στο `/api/progress/{id}`, λίστα αρχείων
- `automation/base.py` — Playwright setup, σύλληψη PDF, `gr_norm()`, `label_norm()`, `debug_dir()`
- `automation/myaade.py` — όλη η λογική πλοήγησης στα portals, ανά έγγραφο
- `templates/index.html` — το UI (Bootstrap, χωρίς build step)

Τα PDF αποθηκεύονται στο `~/Downloads/GovDocs` (ή `DOWNLOADS_DIR`).

## Διάγνωση

Όταν κάτι χαλάει, **πρώτα διάβασε το log** — έχει κάθε βήμα, το URL κάθε
σελίδας και τα labels που βρέθηκαν:

```bash
cat /tmp/gov_doc_fetcher.log
```

Δίπλα του υπάρχουν screenshots (`gov_debug_*.png`) και, όταν αποτυγχάνει ο
εντοπισμός πίνακα, το HTML του (`gov_debug_*.html`). Στα Windows όλα αυτά πάνε
στον προσωρινό φάκελο των Windows — δες `debug_dir()`.

## Παγίδες που έχουν κοστίσει χρόνο

Αυτά είναι πραγματικά bugs που εμφανίστηκαν· μη τα ξανακάνεις.

**Το `os.execv` στα Windows δεν κάνει quoting.** Το `bootstrap()` ξαναμπαίνει
στο venv με `execv`, και σε Windows τα ορίσματα ΔΕΝ μπαίνουν σε quotes: κάθε
διαδρομή με κενό σπάει στο κενό. Σε φάκελο «Δουλειά Ficon» ο interpreter ζητούσε
`Ficon\gov-doc-fetcher\.venv\Scripts\python.exe` και η εφαρμογή δεν ξεκινούσε
καθόλου — και τα «C:\Users\Όνομα Επώνυμο», «Program Files» και OneDrive έχουν
όλα κενά. Στα Windows χρησιμοποιείται `subprocess.run`, το `execv` μένει μόνο
για macOS/Linux.

**Ο ΕΦΚΑ θέλει ΔΙΚΗ ΤΟΥ σύνδεση, το login της ΑΑΔΕ δεν αρκεί.** Σκέτη πλοήγηση
στο `insuranceRequestCommonOper.xhtml` καταλήγει στο
`apps.e-efka.gov.gr/eAccess/login.xhtml`. Η σειρά των οθονών, όπως
καταγράφηκε ζωντανά:

1. `eAccess/login.xhtml` → «Συνέχεια στο TAXISNET»
2. `oauth2.gsis.gr/oauth2server/login.jsp` → κωδικοί TaxisNet
3. `oauth2 …/oauth/authorize` → οθόνη εξουσιοδότησης
4. `eAccess/gsis/login.xhtml?code=…` → καρτέλα «Επιχείρηση/Πολίτης», «Είσοδος»
5. `eAccess/index.xhtml` → «Αποδεικτικό Ασφαλιστικής Ενημερότητας»
6. `eClearanceCertTaxis/faces/secureAll/index.xhtml` → **ΞΑΝΑ** η ίδια επιλογή
7. η φόρμα αιτήσεων

**Η ΦΟΡΜΑ ΑΝΟΙΓΕΙ ΜΟΝΟ ΑΠΟ ΤΟ ΜΕΝΟΥ.** Με απευθείας URL ανοίγει κανονικά, αλλά
ο πίνακας «Στοιχεία Αιτούντος» μένει ΚΕΝΟΣ: το backing bean γεμίζει μόνο όταν
η πλοήγηση γίνει από το μενού. Η υποβολή απορρίπτεται τότε με «Ο Α.Φ.Μ. είναι
υποχρεωτικό πεδίο» — μήνυμα που δείχνει προς τα στοιχεία σύνδεσης, ενώ η
σύνδεση είναι μια χαρά. Κόστισε πολλές ώρες. Το `EFKA_APP_ROOT` δείχνει στο
`eAccess/index.xhtml` ΓΙ' ΑΥΤΟ, και τα `EFKA_MENU_LABELS` δοκιμάζονται με σειρά.

Στο ίδιο μενού υπάρχει «**Έλεγχος Εγκυρότητας** Αποδεικτικού Ασφαλιστικής
Ενημερότητας» — ελέγχει ΞΕΝΟ αποδεικτικό, δεν εκδίδει. Ξεχωρίζει από ένα
γράμμα: «Αποδεικτικού» αντί «Αποδεικτικό».

Στο βήμα 4 η ΠΡΟΕΠΙΛΕΓΜΕΝΗ καρτέλα είναι «Ασφαλισμένος» και ζητά **ΑΜΚΑ**, που
δεν έχουμε και δεν αφορά επιχείρηση. Πρέπει να πατηθεί η καρτέλα
**«Επιχείρηση/Πολίτης»** — φαίνεται αχνή/ανενεργή αλλά πατιέται κανονικά — και
μετά το «Είσοδος» (το ΑΦΜ είναι ήδη συμπληρωμένο). Στην ίδια οθόνη υπάρχει και
**«Αποσύνδεση»**: γι' αυτό τα labels στο `_reach_efka_form()` είναι ΑΚΡΙΒΗ, και
σε άγνωστη οθόνη σταματάμε αντί να μαντέψουμε κλικ.

Η αποτυχία σύνδεσης πετάει `RuntimeError`, ΟΧΙ `DocumentNotAvailable`: με το
δεύτερο εμφανιζόταν στη σύνοψη ως «ℹ️ Δεν υπήρχαν: Ασφαλιστική Ενημερότητα»,
που έλεγε ακριβώς το αντίθετο από την αλήθεια. Το ίδιο ισχύει και για
απορριφθείσα υποβολή.

**Το «Σύνδεση» δεν είναι το πρώτο κουμπί της σελίδας.** Στο `oauth2.gsis.gr` το
κουμπί αλλαγής γλώσσας («English»/«Ελληνικά») είναι `<button type="submit">` και
βρίσκεται ΠΑΝΩ από τη φόρμα. Το `wait_for_selector` με λίστα selectors χωρισμένη
με κόμματα γυρίζει το ΠΡΩΤΟ ΣΤΗ ΣΕΙΡΑ ΤΟΥ DOM, όχι το πρώτο της λίστας — οπότε
πατιόταν η αλλαγή γλώσσας, η σελίδα ξαναφόρτωνε στα αγγλικά με τα πεδία
συμπληρωμένα, και έμοιαζε με λάθος κωδικό. Το `_submit_gsis_form()` ψάχνει το
κουμπί ΜΕΣΑ στη φόρμα που περιέχει το πεδίο του κωδικού (`pw.form`).

**Η εξουσιοδότηση δεν έχει κουμπί «Συμφωνώ».** Τα «Επιστροφή» και «Συνέχεια»
είναι **RADIO BUTTONS** (το «Συνέχεια» ήδη επιλεγμένο) και η υποβολή γίνεται με
το κουμπί **«Αποστολή»**. Πατώντας το «Συνέχεια» σαν κουμπί, ξαναεπιλεγόταν το
ίδιο radio και ο βρόχος γύριζε 8 φορές στην ίδια οθόνη. Και τα δύο radio είναι
στην ΙΔΙΑ γραμμή, οπότε το κείμενο του γύρω στοιχείου είναι «Επιστροφή Συνέχεια»
ΚΑΙ ΓΙΑ ΤΑ ΔΥΟ — με ασαφές ταίριασμα διαλέγεται η «Επιστροφή». Το
`_efka_consent()` ταιριάζει με `label[for=…]`, ακριβώς.

**PrimeFaces: τα dialog υπάρχουν στο DOM ΠΡΙΝ εμφανιστούν.** Και το `innerText`
στοιχείου που ΔΕΝ εμφανίζεται επιστρέφει κανονικά το κείμενό του (το πρότυπο
λέει: αν δεν γίνεται render, `innerText` = `textContent`). Άρα:

- το «OK» κρυφού dialog ταίριαζε, και το κλικ περίμενε 30″ σε κάτι που δεν θα
  γινόταν ποτέ ορατό· το `_click_near()` προτιμά ΟΡΑΤΑ και αρνείται να πατήσει
  αόρατο,
- ο αριθμός αίτησης διαβαζόταν από το ΑΔΕΙΟ πρότυπο του dialog.

Όποιος έλεγχος ρωτάει «τι λέει η οθόνη» πρέπει να φιλτράρει ορατότητα, και να
αγνοεί `<script>`: οι JSF σελίδες κουβαλούν δεδομένα μέσα τους, και ο πίνακας
«Στοιχεία Αιτούντος» φαινόταν γεμάτος ενώ ήταν άδειος.

**Ο αριθμός αίτησης διαβάζεται ΠΡΙΝ πατηθεί το «OK».** Με το OK το popup φεύγει
και ο αριθμός δεν ξαναεμφανίζεται. Είναι το ΜΟΝΟ πράγμα που ξεχωρίζει τη δική
μας γραμμή στη λίστα «Υποβληθείσες Αιτήσεις **τελευταίων 2 ημερών**» — χωρίς
αυτόν, το «Έλεγχος Αποτελέσματος» άλλης γραμμής κατεβάζει ΑΛΛΟ έγγραφο με σωστό
όνομα αρχείου. Γι' αυτό γράφεται και σε αρχείο (`_note_request()`), δίπλα στα
PDF: με αυτόν ανακτάται η βεβαίωση χωρίς νέα υποβολή.

Ο τίτλος του popup είναι σε δικό του στοιχείο και ο αριθμός σε διπλανό: το «πιο
στενό στοιχείο που περιέχει τον τίτλο» δεν έχει ΠΟΤΕ αριθμό. Ζητάμε το πιο στενό
που περιέχει ΚΑΙ τα δύο. Ίδιο μοτίβο και στο `_efka_applicant_filled()`.

**Μετά την υποβολή, ΠΟΤΕ `page.reload()`.** Η σελίδα είναι αποτέλεσμα POST του
JSF: το reload ξαναστέλνει την υποβολή, δηλαδή δημιουργεί ΔΕΥΤΕΡΗ ΑΙΤΗΣΗ. Η
ανανέωση γίνεται με GET στο ίδιο URL.

**Το JSF ξαναγράφει τη φόρμα σε κάθε αλλαγή.** Τα σημάδια (`data-gdf-box`)
χάνονται μαζί με το παλιό element, οπότε ο έλεγχος «έμεινε τσεκαρισμένο;»
ξαναβρίσκει το κουτάκι από την ΕΤΙΚΕΤΑ του — και ΜΕΤΑ από αναμονή, αλλιώς
διαβάζει την κατάσταση πριν απαντήσει ο server. Για τον ίδιο λόγο η αιτία
ξαναελέγχεται πριν την «Υποβολή», με **read-only** έλεγχο
(`_labeled_box_checked()`): το `_check_labeled_box()` ΠΑΤΑΕΙ, και σε ήδη
τσεκαρισμένο κουτάκι θα το ξετσεκάριζε.

**Τα διαγνωστικά ΔΕΝ τυπώνουν τιμές πεδίων.** Το `_tile_choices()` επιστρέφει
και το `el.value`, και στην οθόνη σύνδεσης το πεδίο του κωδικού είναι γεμάτο:
ο κωδικός του πελάτη κατέληξε καθαρό κείμενο στο `gov_doc_fetcher.log`. Πλέον το
`txt()` αγνοεί `type=password` και οι γραμμές διάγνωσης παραλείπουν τα πεδία
εισαγωγής. Μην το χαλαρώσεις.

**Οι ενημερότητες δεν έχουν έτος.** Φορολογική, ασφαλιστική και μητρώο
εκδίδονται για τη ΔΕΔΟΜΕΝΗ ΣΤΙΓΜΗ — δεν υπάρχει «ενημερότητα του 2023». Είναι
στο `YEAR_INDEPENDENT_DOCS`, το `_doc_label()` δεν βάζει έτος στα μηνύματα, τα
αρχεία φέρουν ημερομηνία (`dated_filename`), και όταν ζητούνται ΜΟΝΟ αυτά δεν
απαιτείται καμία χρονολογία — ούτε στο UI ούτε στο `main._run`.

**e-ΕΦΚΑ: μία αιτία ανά υποβολή, και η υποβολή δεν αναιρείται.** Η σελίδα
`insuranceRequestCommonOper.xhtml` λέει ρητά στις οδηγίες της «αφού πρώτα
επιλέξετε μια μόνο αιτία κάθε φορά». Ο χρήστης διαλέγει πολλές στο UI και το
`download_asfalistiki()` κάνει ΞΕΧΩΡΙΣΤΗ υποβολή για καθεμία, με καθαρή
επαναφόρτωση ενδιάμεσα. Αν μια αιτία αποτύχει ΜΕΤΑ την υποβολή, ΔΕΝ
ξαναϋποβάλλεται — η αίτηση έχει ήδη καταχωρηθεί και επανάληψη δημιουργεί
διπλή· το λέμε στον χρήστη και προχωράμε.

Το κουμπί «Υποβολή» επιλέγεται με `_click_near(..., "Είδος Ασφαλ.
Ενημερότητας")`: δίπλα του υπάρχει «Καθαρισμός», και η λέξη «Υποβολή» υπάρχει
και στις οδηγίες πιο κάτω. Το `_click_near` ΔΕΝ περνά από το `NEVER_CLICK`,
οπότε η προστασία των portals της ΑΑΔΕ μένει ανέπαφη — μη μπεις στον πειρασμό
να χαλαρώσεις το `NEVER_CLICK` γι' αυτό.

Οι αιτίες αντιστοιχίζονται με ΔΙΑΚΡΙΤΟ ΑΠΟΣΠΑΣΜΑ, όχι με ολόκληρο το κείμενο
(είναι τεράστια και σπάνε σε γραμμές). Πρόσεχε ότι το «Μεταβίβαση αυτοκινήτου
ΔΧ» συνυπάρχει με το «…(πλην αυτοκινήτων Δ.Χ.)» — υπάρχει test που το φυλάει.

**Στα tests, επανάφερε ό,τι κάνεις monkey-patch.** Το `test_certificate_tile`
αντικαθιστούσε το `_click_and_follow` με κενή συνάρτηση και ΔΕΝ το επανέφερε:
από εκεί και κάτω κάθε test «πατούσε» χωρίς να πατάει τίποτα, και περνούσε
ελέγχοντας μόνο ποιο στοιχείο σημαδεύτηκε. Χρησιμοποίησε `try/finally`.

**Μην κλείνεις το popup της λήψης.** Το `page.close()` σε popup που δεν έκανε
ΠΟΤΕ commit πλοήγηση —το tab που ανοίγει το portal μόνο για να κατεβάσει
αρχείο— **δεν επιστρέφει ποτέ**. Δεν πετάει εξαίρεση, οπότε κανένα try/except
δεν το πιάνει: η λήψη πάγωνε σιωπηλά στο Μητρώο, μετά το «Έκδοση», και δεν
έφτανε ποτέ στη φορολογική ενημερότητα ούτε έβγαινε σύνοψη. Το άδειο tab το
καθαρίζει το `cleanup()` μαζί με τον browser. Ανεξάρτητα από αυτό, το `run()`
έχει πλέον `DOC_TIMEOUT` ανά έγγραφο ώστε ένα κόλλημα να μη ρίχνει όλη τη λήψη.

**Ο πακεταρισμένος Chromium δεν ξεκινά παντού.** Ο Chromium 129 του Playwright
1.47 πετά «spawn UNKNOWN» σε Windows 11 build 26200 (side-by-side σφάλμα στο
manifest του chrome.exe — δες Event Viewer, πηγή SideBySide). Δεν φταίει ούτε
κατεστραμμένο κατέβασμα (δοκιμάστηκε `playwright install --force`) ούτε το VC++
runtime. Γι' αυτό το `launch_browser()` πέφτει στον Edge και μετά στον Chrome
του συστήματος — είναι κι αυτοί Chromium, η λογική πλοήγησης δεν αλλάζει.
Χρησιμοποίησέ το ΠΑΝΤΑ αντί για σκέτο `p.chromium.launch()`, και στα tests.

**Python 3.9–3.12, όχι νεότερη.** Το Playwright 1.47 καρφώνει `greenlet==3.0.3`,
που έχει wheels μέχρι cp312. Σε 3.13/3.14 το pip πάει να το μεταγλωττίσει και
βγάζει 119 γραμμές σφάλματος compiler, χωρίς καμία ένδειξη ότι το πρόβλημα είναι
η έκδοση. Το `run.py` κόβει νωρίς με καθαρό μήνυμα. Αν ανεβάσεις το Playwright,
ξαναδές αυτό το όριο.

**Ελληνικό κείμενο.** Το `upper()` ΚΡΑΤΑΕΙ τους τόνους, άρα
`'ΥΠΟΒΛΗΘΕΙ' in 'υποβληθεί'.upper()` είναι `False`. Χρησιμοποίησε πάντα
`gr_norm()`. Για labels του portal χρησιμοποίησε `label_norm()`, που επιπλέον
μετατρέπει τα **λατινικά ομοιογράμματα** σε ελληνικά: το portal γράφει άλλοτε
«Ε3» με ελληνικό Ε και άλλοτε «E3» με λατινικό, και άλλοτε «v.4172» με λατινικό
v. Πρόσεχε και τις αρνήσεις: το «Δεν έχει υποβληθεί» περιέχει «υποβληθεί».

**Τα κουμπιά δεν είναι κουμπιά.** Πολλά «κουμπιά» του portal είναι `<div>` ή
`<img>`, όχι `a`/`button`/`input`. Ψάξιμο με labels τα χάνει εντελώς. Γι' αυτό ο
εντοπισμός γίνεται από τη **δομή του πίνακα** (`_action_cells`): βρες την
κεφαλίδα «Ενέργειες», πάρε τον δείκτη της στήλης, και μέσα στο κελί βρες το
κλικαρίσιμο χωρίς να υποθέτεις τύπο στοιχείου.

**Πολλές ενέργειες στο ίδιο κελί.** Η γραμμή δήλωσης Ν έχει «Υποβολή τροπ/κής»,
«Προβολή», «Προβολή Ε2», «Προβολή Ε3», «Προβολή TAXISNet» κ.ά. Ζήτα πάντα
ΣΥΓΚΕΚΡΙΜΕΝΗ ενέργεια — η αντιστοίχιση δοκιμάζει πρώτα ακριβές label, ώστε το
«Προβολή» να μη μπερδεύεται με το «Προβολή Ε3».

**Ενέργειες που αλλάζουν κατάσταση.** Το `NEVER_CLICK` μπλοκάρει ΥΠΟΒΟΛΗ,
ΟΡΙΣΤΙΚΟΠΟΙΗΣΗ, ΔΙΑΓΡΑΦΗ, ΑΚΥΡΩΣΗ, ΠΛΗΡΩΜΗ, ΑΠΟΣΤΟΛΗ. Είναι ζωντανό φορολογικό
portal — μη το χαλαρώσεις χωρίς λόγο. Το «Υποβολή τροπ/κής» είναι το ΠΡΩΤΟ
κουμπί του κελιού, πριν το «Προβολή».

**Τα σημάδια χάνονται στην πλοήγηση.** Το `_action_cells` σημαδεύει στοιχεία με
`data-gdf-click`. Μετά από κάθε πλοήγηση η σελίδα ξαναφορτώνει και τα σημάδια
σβήνονται — ξανασάρωσε, μη κρατάς δείκτες από προηγούμενη σάρωση.

**Κελιά-περιτυλίγματα.** Η κεφαλίδα πίνακα πρέπει να ταιριάζει **ακριβώς**
(«Ενέργειες»), όχι με `includes`: τα κελιά layout περιέχουν όλο το κείμενο της
σελίδας, άρα και τη λέξη, και επιλέγεται λάθος πίνακας. Επίσης οι γραμμές που
έχουν μόνο κουμπί δίνουν κενό `innerText` (τα `value` των input δεν μετρούν) —
είναι περιτυλίγματα, όχι δεδομένα.

**Σκελετός σελίδας.** Η μπάρα «Έχετε N νέα μηνύματα. Πατήστε **προβολή**…»
υπάρχει σε κάθε σελίδα και έχει link «προβολή». Αποκλείεται μέσω
`CHROME_ROW_PATTERNS` — αλλιώς κατέβαινε το inbox αντί της δήλωσης.

**print-to-PDF.** Απαγορευμένο (`ALLOW_PRINT_TO_PDF = False`). Σε ορατό browser
το `page.pdf()` κλείνει ΟΛΟΚΛΗΡΟ τον browser και χάνονται τα επόμενα έγγραφα·
και όταν δούλευε, έσωζε τη λάθος σελίδα (το μενού αντί για το Ε3) με σωστό
όνομα αρχείου. Καλύτερα καθαρή αποτυχία από λάθος έγγραφο.

**Έτος στα ονόματα.** Δύο διαφορετικές συμβάσεις:
- webtax (Ε1/Ε3/Εκκαθαριστικό ατομικής): «ΔΗΛΩΣΕΙΣ ΕΤΟΥΣ 2025» = φορ. έτος 2024
  → `safe_filename(...)` αφαιρεί 1
- income (Ν, Ε3 νομικού) και ΦΠΑ: η σελίδα δίνει ρητά ΦΟΡΟΛΟΓΙΚΟ έτος
  → `shift_year=False`

**Απουσία ≠ βλάβη.** Ένα έγγραφο που δεν υπάρχει (νομικό πρόσωπο χωρίς Ε1,
απαλλασσόμενος χωρίς Φ2) πετάει `DocumentNotAvailable`, εμφανίζεται ως `ℹ️` και
δεν μετριέται ως αποτυχία στη σύνοψη.

## Κωδικοί

Οι κωδικοί TaxisNet έρχονται από τη φόρμα, χρησιμοποιούνται στο login και **δεν
αποθηκεύονται πουθενά** — ούτε σε αρχείο, ούτε στο log. Μη προσθέσεις logging
που τους τυπώνει. Τα κατεβασμένα PDF και τα logs είναι στοιχεία πελατών: το
`.gitignore` αποκλείει `*.pdf`, μη το χαλαρώσεις.
